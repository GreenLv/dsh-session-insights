# v0.3.0 release acceptance record

Status: released as `v0.3.0` from commit `d6065402dcae195eba6f6cd8cdd453bc8129b9f1` on 2026-08-25. This change set is presentation-facing (dashboard layout, usage cards, unit/legend fixes) plus parser-level per-session counters; it is platform-neutral.

## Scope

- Dashboard layout overhaul: unified card system, aligned filter toolbar, document-order scroll-aware TOC chip navigation, KPI reorder, active-time hours unit, `会话明细` (Session details) rename, color accents.
- Usage section cards: Skill usage and plugin/MCP usage, alongside session types, tools, file types, and active hours.
- Deterministic analyzers now record per-session `skill_calls` and export `skill_usage` / `plugin_usage` aggregates (parser contract DSH `0.2.2` / sibling `6.0.4`).
- README 'What you get' table and light-theme screenshots refreshed.

## macOS acceptance evidence

### Offline regression (macOS, Python 3.12)

| Gate | Result |
|---|---|
| Python tests | 37 passed |
| Node plugin tests | 11 passed |
| Fixture consistency (`build_fixture.py --check`) | Pass |
| Public-tree audit | Pass (after removing a distribution-ledger wording that tripped the repo's name rules) |
| Installed-runtime `doctor` | Pass |

### Isolated native readback (deterministic subset)

Verified with `scripts/verify_native_acceptance.py --root /tmp/acceptance` against an isolated DSH home, using the installed `0.3.0` runtime:

- `skill-discovery`: pass — the isolated session log contains a `skill` call to `dsh-session-insights`.
- `doctor`, `deterministic-cache` (first report miss `1`, second report hit `1`, third report +`--refresh-deterministic-cache` invalidation `1`), `redacted-privacy` (no sensitive-path / synthetic-key / api-key / bearer sentinels in any report), `output-boundary` (reports written outside `$DSH_HOME/sessions`), `process-cleanup`: all pass.

The verifier's only remaining error is `required acceptance checks are missing or not pass`, which reflects the three **real-model** gates — `semantic-complete`, `metrics-skip`, `fallback` — marked `not_run`, not a deterministic failure.

### Not run in this pass

- Real-model semantic round trip (`semantic-complete`, `metrics-skip`, `fallback`): requires a DSH model-provider run and is the remaining macOS evidence item.
- Windows native verification: covered by the known, unchanged boundaries below.

## Package identity

- Source basis: release commit `d6065402` (tag `v0.3.0`, annotated).
- npm `dsh-session-insights@0.3.0` published; `latest` resolves to `0.3.0`; `dist.shasum` = `7d89604458ff9a15bc3f50d0ab4ef40c3a299d3f`, which matches the local `npm pack --dry-run` shasum for the release commit.
- GitHub Release `v0.3.0` published as a non-draft, non-prerelease release (read back 2026-08-25).

## Remaining acceptance boundaries

1. Deterministic slash dispatch, rendered English DOM, plugin removal, and concurrent-home behavior remain explicit unverified native Windows boundaries (unchanged from `v0.2.0`).
2. The semantic (real-model) gates were not re-run for `v0.3.0`; `v0.2.0` remains the last full semantic readback.
3. Marketplace, article, and outreach status are distribution concerns tracked in [`docs/distribution.md`](../distribution.md), not product-acceptance evidence or `v0.3.0` blockers.
