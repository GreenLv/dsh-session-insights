# v0.3.0 acceptance summary

Status: cross-platform acceptance of `dsh-session-insights` **v0.3.0** (commit `d6065402`, tag `v0.3.0`) is complete as of 2026-08-25. This page reconciles the platform records; evidence details live in [`v0.3.0-candidate.md`](v0.3.0-candidate.md) (macOS) and [`windows-v0.3.0.md`](windows-v0.3.0.md) (Windows).

## Outcome per gate

| Gate | macOS | Windows |
|---|---|---|
| Offline regression (37 Python / 11 Node tests, fixture, public-tree audit, doctor) | Pass | Pass |
| Isolated native readback (`verify_native_acceptance`) | Deterministic 6 gates pass (semantic `not_run`) | Pass (`status=pass`, 7 artifacts) |
| Semantic real-model round trip (`en` + `zh-CN`, `status=complete`, `fallback_reason=null`) | Not run | Pass (model-reasoned, validator-accepted) |
| Rendered English DOM (zero CJK outside `<script>` data) | Not run | Pass (DOM scan + screenshot) |
| Deterministic report generation → correct directory | Pass | Pass |
| Interactive `/session-insights` slash dispatch in a live plugin-loaded DSH Web profile | Unverified | Unverified |

Remaining boundaries: interactive slash dispatch; re-test of the `v0.2.0` edges (concurrent isolated home, plugin removal/rescan, resource-path aliases); marketplace/outreach status is a distribution concern in [`docs/distribution.md`](../distribution.md).

## Publication readback

- npm `dsh-session-insights@0.3.0` published; `latest` resolves to `0.3.0`; `dist.shasum` matches the local release-commit pack (`7d89604458ff9a15bc3f50d0ab4ef40c3a299d3f`).
- Annotated tag `v0.3.0` → commit `d6065402`; GitHub Release `v0.3.0` published (non-draft, non-prerelease).
- The sibling cross-machine skill-sync consumer index pins this release (`v0.3.0` / `d6065402`).
