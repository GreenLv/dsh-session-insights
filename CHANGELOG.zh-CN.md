# 更新日志

[English](CHANGELOG.md)

这里按时间倒序记录公开版本。

## 0.3.0 - 2026-08-25

0.3.0 重新整理了仪表盘布局（统一卡片、对齐的筛选工具栏、滚动跟随的导航药丸），新增 Skill 使用与插件/MCP 使用视图，活跃时间统一以小时为单位，并在确定性分析器中记录会话级 skill 调用。

### 变化

- 仪表盘视觉体系：所有区块统一卡片化（含任务族列表、摩擦信号、代表性工作流、脱敏证据与覆盖说明），垂直间距统一为 28px，同排卡片等高且操作按钮置底，850px 以下降为单列；筛选工具栏重排为六列表单栅格，「工作分类」与「重置筛选 / 导出 JSON / 导出 Markdown」按钮对齐在同一行。
- KPI 按关注度重排：活跃时间、独立 Token 用量、会话数（原「Rollout 数」）、轮次数、工具调用；活跃时间统一以小时为单位；「任务分类钻取」更名为「会话明细」。
- 导航：吸顶目录改为卡片式药丸标签，按文档顺序排列（任务族在时段对比之前）并支持滚动高亮跟随，新增「会话明细」「证据」入口。
- 色彩体系：状态徽标按完成/中止/未完成使用绿/红/黄淡色，四个使用统计卡片使用不同主题色，「一眼看懂」卡片循环配色。
- 数字格式跟随报告语言（英文 1.4B / 中文 1.4亿），不再受浏览器本地化影响。

### 新增

- 交互模式新增「Skill 调用」「插件/MCP 调用」「Web 搜索」统计（随筛选器实时计算）；确定性分析器新增会话级 `skill_calls` 字段（解析契约升级为 DSH 0.2.2 / 6.0.4）。
- 使用方式新增「Skill 使用」「插件/MCP 使用」两张卡片，与会话类型/常用工具同级，展示最常用的 skill 与插件/MCP 服务（分析器导出 top 使用量）。

### 修复

- 图表：每日活跃时间纵轴以小时为单位并标注单位，横轴每个日期都有标签（超过 12 天时均匀抽样），图表置于独立卡片中。
- 建议卡片与工作分类卡片的操作行对齐（复制提示词 + 采用状态、查看此类会话），并固定在卡片底部。
- 任务族列表与「会话明细」采用相同的「标题自适应 + 元信息右对齐」列宽，完成证据整行铺开。
- 滚动定位不再跳章、会话明细与证据区域也有高亮；锚点跳转不会被吸顶栏遮挡。

## 0.2.0 - 2026-08-22

0.2.0 把会话复盘变成 DeepSeek Harness 的原生工作流：运行 `/session-insights`，由当前 DSH Agent 分批分析有界数据，再打开生成在本机的 HTML 报告和配套 JSON。早期的文件日志 CLI 仍然保留。

### 新增

- DSH Bundle 通过 DSH 的 `sessionQuery` 服务读取会话历史，并用六个有界工作流工具协调复盘过程。
- 在插件模式下，原始会话快照通过管道传给 Python 分析器，不会复制到运行结果目录。
- 中英文 Dashboard 使用同一份报告数据，但界面文字和生成叙述分别使用对应语言。

### 变化

- 模型辅助复盘成为 Bundle 的默认路径。使用 `--deterministic` 可生成不调用模型的报告，使用 `--resume` 可继续最近一次尚未完成的有界运行。
- 已发布 Bundle 可以通过 DSH 的 npm 插件渠道安装。

### 修复

- 英文报告不再因为按顺序替换文本而混入中文框架文字；静态界面和生成内容都会明确遵循指定语言。
- 继续运行、取消 Python 子进程、无效模型输出和确定性回退现在会一致地清理运行状态。
- macOS 的 `/var` 与 `/private/var` 别名、包含空格的路径和 Windows 项目筛选会按各平台原生路径规则处理，不再静默拒绝或选择错误会话。
- 语义提示现在会列出所有允许的聚合值，避免模型给出本来有效的答案，却因为无法获知完整约定而触发回退。

发布身份、软件包校验和、CI、原生平台证据与剩余 Windows 限制见 [v0.2.0 验收记录](docs/acceptance/v0.2.0-candidate.md)。

## 0.1.0 - 2026-08-21

首个版本读取 DSH 文件日志，生成可离线打开的工作流复盘 Dashboard 和配套 JSON。它既能完全确定性运行，也可以加入可恢复的模型辅助分析阶段。

### 新增

- 文件日志适配器、Python CLI、受管 DSH Skill 安装、合成测试数据和三种隐私模式。
- 可恢复的语义分析批次、输出校验、缓存，以及生成结果无效或不可用时的明确回退。

### 修复

- Windows 控制台启动器直接在最终环境路径创建，不再保留已删除临时目录的引用。
- 完整的 `<redacted>` 占位符可以通过隐私检查，真实凭据值仍会被拒绝。

### 平台范围

- 仓库检查、跨平台 CI，以及 macOS 和 Windows 11 的隔离原生验收均已通过。该版本没有完成 Linux 原生验收、真实会话 smoke、真实模型语义往返或 Windows DSH GUI Skill 重新扫描。
